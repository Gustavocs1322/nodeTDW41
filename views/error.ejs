<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Star Wars</title>
    <style>

        .error{

            top: 5%;
            left: 5%;
            right: 5%;
            bottom: 5%;
        }


        /* Estilos generales */
        body {
            margin: 0;
            font-family: 'Arial', sans-serif;
            background: #000;
            color: #FFF;
            overflow: hidden;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-image: url('https://example.com/galaxy-background.jpg'); /* Fondo de galaxia */
            background-size: cover;
            background-position: center;
        }

        /* Texto de error */
        .error-message {
            font-size: 5em;
            color: #FFD700;
            text-shadow: 0 0 10px #FFD700, 0 0 20px #FFD700, 0 0 30px #FF4500;
            margin: 0;
            animation: flicker 1.5s infinite alternate;
        }

        /* Texto de subtítulo */
        .subtitle {
            font-size: 1.5em;
            color: #FFFFFF;
            margin-top: 0;
            animation: text-fade 3s infinite alternate;
        }

        /* Animación de parpadeo */
        @keyframes flicker {
            0% { opacity: 1; }
            50% { opacity: 0.8; }
            100% { opacity: 1; }
        }

        /* Animación de desvanecimiento */
        @keyframes text-fade {
            0% { opacity: 1; }
            100% { opacity: 0.5; }
        }

        /* Efecto de estrella */
        .stars {
            position: absolute;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            pointer-events: none;
        }

        /* Estilos para las estrellas */
        .stars::after {
            content: '';
            position: absolute;
            top: 0;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, #FFFFFF 2%, transparent 4%);
            background-size: 2px 2px;
            opacity: 0.5;
            animation: star-move 100s linear infinite;
        }

        /* Movimiento de estrellas */
        @keyframes star-move {
            0% { transform: translate3d(0, 0, 0); }
            100% { transform: translate3d(-100%, -100%, 0); }
        }
    </style>
</head>
<body>
    <div id="error">
        <h2><%= mensaje %></h2>
        <h1 class="error-message">ERROR</h1>
        <p class="subtitle">It's not the page you're looking for...</p>
        <div class="stars"></div>
    </div>
</body>
</html>
